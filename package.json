{
  "name": "nightvision-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true})\"",
    "dev": "cd frontend && npm run dev"
  }
}
